{{- /* PawMatch Service */ -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pawmatch.fullname" . }}
  labels:
    {{- include "pawmatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pawmatch.selectorLabels" . | nindent 4 }}